import Controller from 'contracts:controller.js';
import { Json } from 'http:response/Json.js';

export default class extends Controller {
  constructor() {
    super([
      // ... modules to dependency inject
    ]);
  }

  invoke(_request) {
    return new Json(null, Json.STATUS_OK);
  }
};

